#include <stdio.h>

int main()
{
    char wrd[20];
    printf("Input the word: ");
    scanf("%s", wrd);

    int i;

    for (i = 0 ; wrd[i] != '\0' ; i++)
    {
        printf("%c", wrd[i]);
    }
    printf("\n\n");
    
return 0;
}
